

Commands:- 

Mumbai region instance Commands
sudo sudo
yum update -y
yum install git -y
git --version
git config --global user.name "Dnyaneshwar"
git config --global user.email "todmaldnyanu98@gmail.com"

Singapore region instance commands:-
 
sudo sudo
yum update -y
yum install git -y
git --version
git config --global user.name "vaibhav"
git config --global user.email "todmaldnyanu85@gmail.com"
git config --list << to see the details >>


How to commit push, and pull from GitHub

-->> Login into Mumbai Ec2 instance <<--


->> create one directory mkdir mumbaigit
->> git init
->> touch myfile (put some data)
->> git status
->> git add . or git add <filename>
->> git commit -m "<enter some message here>"
->> git status
->> git log --oneline
->> git show <commit id>
->> git remote add origin master
->> git push origin master 

-->> login into Singapore instance -->>

->> create one directory mkdir singaporegit
->> git init
->>  git remote add origin <GitHub repo URL>
->> git pull origin master
->> git log --oneline
->> git show <commit id>\
->> git status
->> git add
->> git commit -m "<enter some message here>"
->> git status
->. git log 
->> git push origin master 


***>>>> Ignore command ****

1) create one hidden file (.gitignore) and enter file format which you want to ignore

 ->> for example :- vi .gitignore
                    * css
                    * txt
                    * java
->> git add .ignore
->> git commit -m "ignore file added"
->> git status
->> git add 
->> git status 
->> git commit -m "enter some message here"


***** How to create branch and merge***



